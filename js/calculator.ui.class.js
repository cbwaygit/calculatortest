(function(window) {
	window.CalculatorUI = {
		updateViewer: function (val) {
			// updates the viewer every time a new digit is input, when the equals button is clicked, or when the clear button is clicked
			// hint: use jquery to target the viewer dom-element to update the element's html
		}
	}
})(window);